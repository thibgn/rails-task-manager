<h1>My tasks</h1>
<hr />
<ul>
  <% @tasks.each do |task| %>
  <li>
    <%=task.completed ? "[x] " : "[ ] "%>
    <%= link_to task.title, task_path(task) %>
  </li>
  <% end %>
</ul>
<hr />
<%= link_to 'Add a new task', :new_task %>